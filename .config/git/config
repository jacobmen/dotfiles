[user]
    email = 40839083+jacobmen@users.noreply.github.com
    name = jacobmen

[credential]
    helper = cache --timeout=36000000

[core]
    pager = delta
    compression = 9
    whitespace = error
    preloadindex = true

[interactive]
    diffFilter = delta --color-only
    singlekey = true # don't need to press enter for single key prompts

[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
    side-by-side = true

[commit]
    verbose = true

[status]
    branch = true
    short = true
    showStash = true
    showUntrackedFiles = all

[push]
    autoSetupRemote = true
    default = current # push only current branch by default

[pull]
    rebase = true
    default = current

[rebase]
    autoStash = true
    missingCommitsCheck = warn

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
    colormovedws = "allow-indentation-change"

[log]
    abbrevCommit = true

[gc]
    reflogExpire = 200
    reflogExpireUnreachable = 90
